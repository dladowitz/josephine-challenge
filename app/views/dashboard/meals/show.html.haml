%h1
  Meal Dashboard for
  = @meal.title

%h2 Customers
%ul
  - @orders.each do |order|
    %li= "#{order.user.first_name} #{order.user.last_name}"
%hr

%h2 Revenue
-# TODO create helper methods for these
%h3= "Order Count: #{@orders.count}"
%h3= "Total Revenue: #{number_to_currency @orders.count * @meal.price}"
%hr
