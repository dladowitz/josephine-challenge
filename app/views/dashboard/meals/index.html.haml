%h1
  Meals Dashboard for
  = @user.first_name
  = @user.last_name

%ul
  - @meals.each do |meal|
    %h2
      Meal:
      = link_to meal.title, dashboard_meal_path(meal)

    -# TODO create helper methods for these
    %h3= "Order Count: #{meal.orders.count}"
    %h3= "Total Revenue: #{meal.orders.count * meal.price}"
    %hr
